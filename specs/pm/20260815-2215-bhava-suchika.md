# Bhava Suchika (භාව සුචික) Verification Report

**Date:** 2026-08-15
**Verifier:** PM/QA verification pass (read-only; no code changes made)
**Repo:** `kendara`
**Scope:** Verify the implemented Bhava Suchika (Navamsa house-index) feature against the five Bhava Suchika specs and the confirmed product rules; audit declared deviations and out-of-scope changes; report verdict, traceability, and issues.

**Specs under review:**
- `specs/business-analysis/20260814-2055-bhava-suchika.md` (US-BS-001..008, 381 lines)
- `specs/architecture/20260814-2127-bhava-suchika-architecture.md` (D1–D9)
- `specs/ux/20260814-2130-bhava-suchika.md`
- `specs/qa/20260814-2210-bhava-suchika-test-plan.md` (145 test cases)
- `specs/development/20260815-0801-bhava-suchika.md` (deviation audit)

---

## 1. Verification Result Summary

### CONDITIONAL PASS

The Bhava Suchika feature is **functionally complete and correct at code-inspection level**: all **8 user stories** and all acceptance criteria trace to test IDs and verified source locations, with **zero code-level AC failures found**. The core rule `Bhava Suchika = ((navamsaSign − lagnaSign) mod 12) + 1` is implemented identically across the auto pipeline, the manual path, the render-time legacy fallback, and the search layer.

**One blocking caveat:** the automated suite **could not be executed in this verification environment** (no shell access). The 90-test count for the three in-scope suites is statically verified (50 + 30 + 10), but the suite must be run once by the developer/CI to confirm the claimed `90 passed` before this becomes an unconditional GO.

| Dimension | Result |
|-----------|--------|
| User stories (US-BS-001..008) | 8/8 PASS at code inspection (0 AC failures) |
| Core rule (whole-sign, mod-12) | MATCH across auto / manual / legacy / search |
| D1 (Lagna value never from D9 chart doc) | MATCH — `synthesizeNavamsaCalculation` strips both fields |
| D5 (manual gating matrix) | MATCH — partial-input matrix per architecture |
| Declared deviations (5) | All audited — see §3; none affect acceptance criteria |
| Test count (3 in-scope suites) | 90 statically confirmed (50 + 30 + 10); **not executed here** |
| i18n parity (en/si) | Key parity for all Bhava Suchika strings; 12 SI names match spec exactly |
| Schema / storage contract | Matches data-model (declared deviation: `Mixed` no-default — audited, safe) |

---

## 2. Acceptance-Criteria Traceability Matrix

All 8 BA user stories verified. Status **PASS** = verified by source inspection of the listed locations (and, where the QA plan marks them automated, mapped to test IDs); **PASS\*** = PASS with a noted minor deviation/limitation described under §5.

| User Story | ACs | QA test IDs | Source / evidence | Status |
|-----------|-----|-------------|-------------------|--------|
| US-BS-001 Lagna section tag | 4 | UT-BS-051..064, UI-BS-301/302/303/304, RE-BS-457 | `page.tsx:389` (`resolveLagnaBhavaSuchika`), `page.tsx:1302-1308` (tag FIRST in ascendant flag row; row `null` when no flags and no value), `BhavaSuchikaTag.tsx` (indigo pill `text-indigo-700 bg-indigo-50 border-indigo-200`, CSS tooltip + `title` fallback, `aria-describedby`) | PASS |
| US-BS-002 Planets-table column | 4 | UT-BS-052/054, UI-BS-306..310, RE-BS-456 | `page.tsx:1500-1512` (desktop `<th>` after Nakshatra, label + ⓘ info button `title`/`aria-label`), `page.tsx:1676-1679` (desktop `<td>` `whitespace-nowrap`, `{value} — {name}` or `—`), `page.tsx:1885-1891` (mobile span after Nakshatra, `{label}: {value} — {name}`, omitted when undefined) | PASS* (mobile omits `—` row when undefined — allowed per US-BS-002 edge "empty or —") |
| US-BS-003 Lagna calculation | 4 | UT-BS-001..007, 012..018, 051, 053, 061..064, UT-BS-081/083, RE-BS-450/451/452 | `bhavaSuchika.ts:19-25` (`computeNavamsaLagnaSign`, inline derivation + `1e-9` epsilon), `bhavaSuchika.ts:28-30` (`computeLagnaBhavaSuchika`), `calculation.ts:446` (auto pipeline: `computeLagnaBhavaSuchika(ascSign, computeNavamsaLagnaSign(ascSign, ascLong % 30))`) | PASS |
| US-BS-004 Per-planet calculation | 4 | UT-BS-008..011, 019..025, 052, 054, UT-BS-082/084, RE-BS-455 | `bhavaSuchika.ts:33-47` (`computePlanetBhavaSuchika`, `computeBhavaSuchika` keyed `"1".."9"`), `calculation.ts:447` | PASS |
| US-BS-005 Bilingual 12 names | 5 | BI-BS-180..186, RE-BS-462 | `en.json:349-367` + `si.json:349-367` (key parity: `label`/`tooltip`/`infoGlyph`/`names.1..12`; SI names match spec table verbatim; EN transliterations Lagnamshaka…Vyamshaka), `search.conditions.bhavaSuchika`/`planetBhavaSuchika` parity (`en.json:177-178`, `si.json:177-178`); no hardcoded strings (all resolved via `t()`) | PASS |
| US-BS-006 Manual behaviour | 4 | UT-BS-055..060, IT-BS-103/104/105/105b, RE-BS-459 | `manualChart.ts:1254-1260` (Lagna value iff `navamsaLagna` in [1,12]), `manualChart.ts:1262-1274` (per-planet iff `navamsaHouses` present; empty map → `undefined`), return only when defined (1294-1295) | PASS |
| US-BS-007 Storage / recalc / legacy | 4 | UT-BS-061..064, IT-BS-100/101/102/108/109/110/112, RE-BS-460/463 | Persistence: `CalculatedDetails.ts:106,108` (fields), `api/horoscope/route.ts:80-83` (auto `...calculated` spread), `api/horoscope/manual/route.ts:83-89` + `api/horoscope/[id]/manual-chart/route.ts:79-91` (manual `...synth` spread); recalc: `recalculationJob.ts:193/210` (recompute, no `overridden` merge), `recalculationJob.ts:196/211` (`shadbalaya` merge only); legacy fallback: `bhavaSuchika.ts:84-165` (resolvers); read-only: no bhava route exists (IT-BS-100/RE-BS-460), Shad Bala PATCH `$set` touches only `shadbalaya.*` (IT-BS-112) | PASS |
| US-BS-008 Search | 6 | SR-BS-200..228, RE-BS-459/463..466 | `vocabulary.ts:29` (trigger words SI+EN), `vocabulary.ts:34-47+` (name maps), `route.ts:45-46` (ExactCondition `bhava_suchika`/`planet_bhava_suchika`), `route.ts:279-309` (`getBhavaSuchikaMatch`/`buildBhavaSuchikaMatch`), `route.ts:912-918` (exact via `resolveLagnaBhavaSuchika`/`resolvePlanetBhavaSuchika`), `route.ts:650-664` (scoring +1.0 lagna / +0.5 planet), `route.ts:574-577` (keywords), `textContent.ts:20-46` (sentences both locales, only when resolved), `search/page.tsx:500-507` (condition text), `search/page.tsx:820-824` (tag FIRST in card flag row), `search/page.tsx:1126-1131` (per-planet value after Nakshatra) | PASS* (SR-BS-224/225/226 are component-level, not automated — see §5 risk 3) |

**Traceability verification (from QA plan §16):** 8/8 stories mapped; the automated coverage is 90 tests (UT-BS-001..025 + 051..064 + 081..085 + RE-BS-450..458 in `bhavaSuchika.test.ts`; SR-BS-200..223 + 227/228 + RE-BS-459/463..466 in `search-bhava-suchika.test.ts`; IT-BS-100..112 + RE-BS-460 in `bhavaSuchikaApi.test.ts`). UI/AX/BI/E2E cases remain manual/component per the QA plan flags (no RTL/Playwright configured).

---

## 3. Declared-Deviation Audit

| # | Deviation (per dev spec) | Implementation | Verdict | AC impact |
|---|--------------------------|----------------|---------|-----------|
| 1 | `Schema.Types.Mixed` no-default for `lagnaBhavaSuchika`/`bhavaSuchika` (vs arch `Map default {}`) | `CalculatedDetails.ts:106,108`; resolution layer treats absent → `undefined` → render fallback (`bhavaSuchika.ts:88-93, 117-130`) | **ACCEPTED** — avoids persisting empty `{}` `bhavaSuchika` on legacy/manual-no-navamsa docs; no AC requires a default `{}`. Corrupt stored values warn + degrade to `—`, never guessed (RE-BS-453/454). | None |
| 2 | Resolve helpers typed `unknown` (docs treated as opaque records) | `bhavaSuchika.ts:70-165` — `CalcLike`/`ManualPlacementsLike` + `isValidBhavaSuchikaValue` guards, `console.warn` on corrupt | **ACCEPTED** — `lean()` docs are already untyped; guards centralize the contract shared by page/search/textContent. | None |
| 3 | `+1e-9` epsilon in `computeNavamsaLagnaSign` (vs floor-down) | `bhavaSuchika.ts:21-23` — pins exact double-boundary multiples (e.g. `2*(30/9)` ≈ `1.9999999999999998`) to the upper wedge | **ACCEPTED, PINNED** — matches the mathematical boundary semantics of UT-BS-013/015; real ephemeris degrees never land exactly on a boundary; UT-BS-064 confirms no drift vs `deriveNavamsaLagnaFromDegree` except the pinned exact-boundary case. | None |
| 4 | Multi-planet query pairs with first matched planet | `route.ts:303-309` (`buildBhavaSuchikaMatch` uses `getPlanetMatches(query)[0]`) | **ACCEPTED (declared)** — mirrors the existing planet-strength pairing limitation; single-planet queries (the norm) are exact; SR-BS-213b pins the behavior. | None (documented in QA) |
| 5 | CSS-only tooltip on search cards may clip in `overflow-y-auto` container | `BhavaSuchikaTag.tsx` — native `title` attribute fallback always present | **ACCEPTED** — `title` covers all browsers regardless of clipping; visual nicety only. | None |

**Additional implementation notes (verified, equivalent-or-benign):**
- **Manual derivation location:** dev implemented it in `manualChart.ts` `synthesizeOtherDetails` (1207-1297) rather than `manualChartDetails.ts` `synthesizeCalculation` as the architecture suggested. Outcome is equivalent — both manual API routes spread the result into `CalculatedDetails` (`manual/route.ts:83-89`, `manual-chart/route.ts:79-91`). No AC impact.
- **D9 stripping (D1):** `manualChartDetails.ts:263-267` deletes `lagnaBhavaSuchika`/`bhavaSuchika` from `synthesizeNavamsaCalculation` output (UT-BS-060) — the Lagna value never derives from the D9 chart doc.
- **D5 partial-input matrix:** `navamsaLagna`-only → Lagna present, per-planet absent (UT-BS-058); `navamsaHouses`-only (API edge) → per-planet present, Lagna absent (UT-BS-059). Matches the architecture matrix.
- **EN tooltip wording** (`en.json:351`) differs cosmetically from the UX draft; semantically identical. Domain to confirm alongside the EN transliterations (BA OQ1).
- **Out-of-scope doc moves:** `docs/shadbalaya.md` + `docs/yogakaraka.md` → `docs/done/` (consistent with the existing archive convention — 18 docs already there), `docs/TODO.md`/`docs/student-notes.md` edits, `shadBalayaTooltip.test.ts` reformat. **FLAG-ONLY, not reverted.** `docs/TODO.md:10` still references `@shadhbalaya.md` (pre-existing typo, now dangling after the move).

---

## 4. Test Execution Results

**⚠️ Not executed in this verification pass** — the session has no shell access, so `npx jest` could not be run and pre-existing-failure attribution (via `git stash`) was not possible. The numbers below are **static counts** verified by reading the test files, not runtime results.

| Suite | Tests (static count) | Claims | Evidence read |
|-------|----------------------|--------|---------------|
| `src/__tests__/bhavaSuchika.test.ts` | 50 | UT-BS-001..025, 051..064, RE-BS-450..458, UT-BS-081..085, `isValidBhavaSuchikaValue` | 50 `test(...)` calls confirmed; golden fixture values hand-verified against the formula (see §4a) |
| `src/__tests__/search-bhava-suchika.test.ts` | 30 | SR-BS-200..223, SR-BS-227/228, RE-BS-459/463..466 | 544 lines; embedding + qdrant mocks confirmed at lines 49-56 (mandatory — route calls `generateEmbedding` on every POST) |
| `src/__tests__/bhavaSuchikaApi.test.ts` | 10 | IT-BS-100..112, RE-BS-460 | 365 lines; persistence-spread, manual gating, recalc, no-new-route, Shad-Bala-PATCH regression |
| **Total (3 in-scope suites)** | **90** | **90 passed** | Consistent with developer claim |
| Full suite | — | 719 passed / 11 pre-existing failed (`privacy.test.ts` + `search-combined.test.ts`) | Attribution **unverifiable here** (needs `git stash`); matches the pre-existing failure baseline recorded in earlier dev specs |

**Action required before unconditional GO:** run

```
npx jest src/__tests__/bhavaSuchika.test.ts src/__tests__/search-bhava-suchika.test.ts src/__tests__/bhavaSuchikaApi.test.ts
```

and confirm 90 passed; then `npx jest` to confirm the pre-existing 11 failures are unchanged.

### 4a. Golden-fixture spot-check (hand-verified)

`src/__tests__/fixtures/bhava-suchika-default-2026-08.json` — asc sign 4, degree 4.76. Wedge = `floor(4.76 / (30/9)) + 1` = 2 → `navamsaSign(4, 2)` = 5 (Cancer is movable, +1). Lagna BS = `((5−4) mod 12) + 1` = **2** ✓. Planet values vs `((navamsaSign − 4) mod 12) + 1`: 7→4 ✓, 11→8 ✓, 9→6 ✓, 1→10 ✓, 1→10 ✓, 8→5 ✓, 10→7 ✓, 2→11 ✓, 8→5 ✓. All 10 fixture values consistent with the formula.

---

## 5. Risks / Open Items

| # | Item | Severity | Mitigation / owner |
|----|------|----------|--------------------|
| 1 | **Test suite not executed in this pass** | **Blocking (procedural)** | One `npx jest` run (commands in §4); developer/CI. No code defect observed, but the GO is conditional on it. |
| 2 | **EN transliterations + tooltip wording unconfirmed** (BA OQ1) | Low | Product owner / domain confirms the 12 EN names (currently Lagnamshaka…Vyamshaka) and the tooltip copy. |
| 3 | **SR-BS-224/225/226 (search-card mirroring) not automated** — `jest.config.js` `testMatch` only matches `*.test.ts` (QA flag §10.10); verified by code inspection only | Low | Extend `testMatch` to `*.test.{ts,tsx}` + add RTL tests, or accept as manual checks. |
| 4 | **Legacy fallback rounding drift** — stored `ascendant.degree` is rounded to 2 decimals (`calculation.ts:470-471`), so the fallback could disagree with compute-time if rounding crosses a navamsa boundary (~0.15% of charts) | Low | Same behavior as the existing inline navamsa-lagna path; non-blocking. |
| 5 | **Search-card tooltip clipping** in the `overflow-y-auto` results container | Low | `title` attribute fallback always present (deviation #5). |
| 6 | **Out-of-scope changes** (docs moves/edits, test reformat) | Informational | Owner decision to keep or revert; `docs/TODO.md:10` has a now-dangling `@shadhbalaya.md` reference (pre-existing typo). |
| 7 | **Corrupt-doc / no-flicker browser observations** (RE-BS-453-458, UI-BS-305) | Low | Manual E2E-BS-403/408 checks per QA plan; unit layer pins the guard behavior. |

---

## 6. Go / No-Go Recommendation

### CONDITIONAL GO

- **Code-level verdict:** 8/8 user stories PASS at code inspection; 0 AC failures; all 5 declared deviations audited and accepted (none affect acceptance criteria); D1/D5 rules verified in the source.
- **Condition:** execute the three in-scope suites (`90 passed`) and confirm the full-suite pre-existing 11 failures are unchanged. Until then, treat the test evidence as static-only.
- **Non-blocking follow-ups:** domain confirmation of EN names/tooltip (§5 #2), decision on out-of-scope doc changes (#6), optional RTL coverage for search-card mirroring (#3).

No code changes were made during this verification.
