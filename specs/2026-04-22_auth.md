# Spec — 2026-04-22 Auth

## Context

I need to implement Google SSO for login to the system.

## Goal

The system should modify for implementing SSO. Now need only Google SSO. will need more SSO implementation.

## Backend changes

- Implement needed endpoints.
- Need to use mongo as the db.
- Should use repository pattern for handling db.
- The db connection should possible to inject without creating for each.
- The code should follow MVC architecture.

## Frontend changes

- Need to implement /login route for handling SSO.
- Need to implement /dashboard page. After login should redirect to the dashboard.
- Need change for SSO implementation.

## Acceptance criteria

- After configure the APP properly any google user should possible to login to the dashboard page.
- Newly logged users should save in the db.

## Out of scope

- Should have nice UI for the FE components.
