I need to add new tab with setting tab as Location.

It should list all locations list.
Should possible to add locations.

- Name (need location suggestion via API as now in the horoscope adding page)
- Lat Long value. (should support to copy paste CSV values, Should )
- Public/private

Public places should appear for all. Hospitals are example for public locations.
Private places should appear only for the adding user

The Horoscope adding page should modify to using this locations. The location field should populate the location from this db instead of using API.
User should possible to override the Lat long value if he want.
