## For next step

- Should calculate Rupa values for Shad bala
- Should have setting per student, Then need to store calculated values per student.
- Need to use semantic html everywhere

## Quick fixes

### Manual calculated horoscope issues

- Should possible adding without Navansaka. Then the system should not generate Navansaka or other Warga kendara for such horoscope.
  - So Navansaka related calculation should hidden from calculation tables.
  - Degree's no need to shows.
  - Consider all planet in the house middle degree.  

### search query

### Student notepad

#### In Graha bala section

- Should include conjunctions and aspects of planets in each plant section in Graha bala.
  - As example, In Ravi row - should have conjunction tags like බුධ සමග, කේතු සමග, etc... Should show aspects tags as සඳගේ දෘශ්ඨිය,ගුරු ගේ දෘශ්ඨිය
- tags color should depend on the each planet color. As example Budha has 4/5 in its main tag.Show බුද සමග tag should show as green color. If Budha main tag is changing to red color all budha conjunctions and aspects should show with red color.
- tags tool tip show the conjunction different, and aspect different with its reason as calculation table shows.

- Should show the Bhava suchika value.
  - If it is 6, 8, 12 should show as red color. if it is 1, 5, 9 should show as green color. Otherwise should show as gray color.

- Bhava value is wrong. It should show the house ownership. Now it is showing planets current house value. If planets has two houses should show as two tags.
- Should show the planet house owner tag. tag should color using owners ratio. If house owner has another house it also should show as different tag.If it is 6, 8, 12 should show as red color. if it is 1, 5, 9 should show as green color.  Otherwise should show as gray color.
- Should show the planet Nakshathra owner tag. tag should color using owners ratio. If house owner
